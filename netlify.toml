[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: serve index.html for client-side routes (deep links / refreshes).
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
